# Diccionarios u Objetos (se pueden reasignar los valores de las keys pero no las keys. No tienen indices)
obj = {
    "Clave1": [1, 2, 3,],
    "Clave2": True,
    "Clave3": {
        "perrito": "katty"
    }
}

print(obj)